---
title: "Vue | computed 计算属性 vs methods 方法"
date: "2025-01-20 19:00:00"
description: "② 作为属性，直接使用 →。② 作为方法，需要调用 →。，再次使用直接读取缓存，this.方法名( )"
tags: ["vue.js", "前端", "javascript", "开发语言", "前端框架"]
viewCount: 564
articleId: "145257868"
sourceUrl: "https://blog.csdn.net/jingling555/article/details/145257868"
cover: "https://i-blog.csdnimg.cn/blog_migrate/tags/e545b73b7be5e746d9ee88243b98e157.png"
---#### computed 计算属性 vs methods 方法:

*   **computed 计算属性：**

**作用：**封装了一段对于数据的处理，求得一个结果。

**语法：**

① 写在 computed 配置项中

② 作为属性，直接使用 → this.计算属性 {{ 计算属性 }}

优点：

**缓存特性**（提升性能）：

计算属性会对计算出来的结果缓存，再次使用直接读取缓存，

依赖项变化了，会自动重新计算 → 并再次缓存

![](https://i-blog.csdnimg.cn/direct/d3bb4bc5d69143aabd5a70472609ceae.png)

![](https://i-blog.csdnimg.cn/direct/ea5eb8e900a74a0ebba7e2e3d0235831.png)

实现效果：

![](https://i-blog.csdnimg.cn/direct/cfbdd72e4eff43f5ba1273f82cfa42ec.png)

Vue代码：

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <style>
    table {
      border: 1px solid #f904f9;
      text-align: center;
      width: 240px;
    }
    th,td {
      border: 1px solid #000;
    }
    h3 {
      position: relative;
    }
  </style>
</head>
<body>

  <div id="app">
    <h3>行李清单</h3>
    <table>
      <tr>
        <th>名字</th>
        <th>数量</th>
      </tr>
      <tr v-for="(item, index) in list" :key="item.id">
        <td>{{ item.name }}</td>
        <td>{{ item.num }}</td>
      </tr>
    </table>

    <!-- 目标：统计求和，求得行李总数 -->
    <p>行李总数：{{ totalCount }} 个</p>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/vue@2/dist/vue.js"></script>
  <script>
    const app = new Vue({
      el: '#app',
      data: {
        // 现有的数据
        list: [
          { id: 1, name: '外套', num: 1 },
          { id: 2, name: '裤子', num: 2 },
          { id: 3, name: '裙子', num: 3 },
        ]
      },
      computed: {
        totalCount () {
          // 基于现有的数据，编写求值逻辑
          // 计算属性函数内部，可以直接通过 this 访问到 app 实例
          // console.log(this.list)

          // 需求：对 this.list 数组里面的 num 进行求和 → reduce
          let total = this.list.reduce((sum, item) => sum + item.num, 0)
          return total
        }
      }
    })
  </script>
</body>
</html>
```

*   **methods 方法：**

**作用：**给实例提供一个方法，调用以处理业务逻辑。

**语法：**

① 写在 methods 配置项中

② 作为方法，需要调用 → this.方法名( ) {{ 方法名() }} @事件名\="方法名"

![](https://i-blog.csdnimg.cn/direct/2773a0ecedb44e59b4e57530b31234c3.png)

![](https://i-blog.csdnimg.cn/direct/ac3aeea362144147ad731c3cae149f53.png)

实现效果：

![](https://i-blog.csdnimg.cn/direct/ab4e9c15e84c433782004efc8864739b.png)

Vue代码：

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <style>
    table {
      border: 1px solid #04e0f9;
      text-align: center;
      width: 240px;
    }
    th,td {
      border: 1px solid #000;
    }
    h3 {
      position: relative;
    }
  </style>
</head>
<body>

  <div id="app">
    <h3>行李清单</h3>
    <table>
      <tr>
        <th>名字</th>
        <th>数量</th>
      </tr>
      <tr v-for="(item, index) in list" :key="item.id">
        <td>{{ item.name }}</td>
        <td>{{ item.num }}</td>
      </tr>
    </table>

    <!-- 目标：统计求和，求得行李总数 -->
    <p>行李总数：{{ totalCountFn() }} 个</p>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/vue@2/dist/vue.js"></script>
  <script>
    const app = new Vue({
      el: '#app',
      data: {
        // 现有的数据
        list: [
          { id: 1, name: '外套', num: 1 },
          { id: 2, name: '裤子', num: 2 },
          { id: 3, name: '裙子', num: 1 },
        ]
      },
      methods: {
        totalCountFn () {
          let total = this.list.reduce((sum, item) => sum + item.num, 0)
          return total
        }
      }
    })
  </script>
</body>
</html>
```
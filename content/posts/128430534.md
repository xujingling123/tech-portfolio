---
title: "C语言|模拟实现strcpy函数和strcat函数"
date: "2022-12-25 11:18:48"
description: "【代码】C语言|模拟实现strcpy函数和strcat函数。"
tags: ["c语言", "开发语言"]
viewCount: 155
articleId: "128430534"
sourceUrl: "https://blog.csdn.net/jingling555/article/details/128430534"
---**strcpy:**

> char\* strcpy(char \* destination, const char \* source );

*   源字符串必须以 '\\0' 结束。
*   会将源字符串中的 '\\0' 拷贝到目标空间。
*   目标空间必须足够大，以确保能存放源字符串。
*   目标空间必须可变。

```cpp
char my_strcpy(char* dest, const char* src) {

	while (*dest++ = *src++)
	{
		
	}
	return dest;
}
```

**strcat:**

> char \* strcat ( char \* destination, const char \* source );

*   源字符串必须以 '\\0' 结束。
*   目标空间必须有足够的大，能容纳下源字符串的内容。
*   目标空间必须可修改。

```cpp
char my_strcat(char* dest, const char* src) {
	char* ret = dest;
	assert(dest != NULL);
	assert(src != NULL);
	while(*dest) {
		dest++;
	}
	while (*dest++ = *src++)
	{

	}
	return ret;
}
```
---
title: "uniapp项目使用ucharts实现折线图详细讲解（案例）"
date: "2025-07-30 09:44:20"
description: "本文介绍了在HBuilderX中使用uCharts图表库的安装和使用方法。首先通过插件市场安装uCharts，然后提供了完整的Vue3+TS实现示例代码，包括模板结构、类型定义、响应式数据配置和图表选项设置。文章特别说明了Vue2项目需要转换为Vue3写法，建议使用AI辅助转换，并展示了如何将配置项抽离为独立文件以优化代码结构。最后演示了在微信小程序端实现折线图的效果，为开发者提供了完整的图表实现方案。"
tags: ["uni-app", "笔记", "开发语言", "typescript", "前端框架", "前端", "vue.js"]
viewCount: 1179
articleId: "149768014"
sourceUrl: "https://blog.csdn.net/jingling555/article/details/149768014"
cover: "https://i-blog.csdnimg.cn/direct/96f9c8d8e52d41049d51882b1616bbf0.png"
---### 1.在Hbuildx里面的工具>插件安装，进入DCloud搜索uchart

![](https://i-blog.csdnimg.cn/direct/2fa4b3bd6a9142bfb28609fc4860659a.png)

![](https://i-blog.csdnimg.cn/direct/605024bf076e43ca882f93b269602982.png)

### 2.点击对应的项目导入该插件

![](https://i-blog.csdnimg.cn/direct/6af15e73fb294098bf549165c3bf4de1.png)

![](https://i-blog.csdnimg.cn/direct/3dfff6fb24e04e83a9da52180e33bc14.png)

可以看到在该目录下有该插件

![](https://i-blog.csdnimg.cn/direct/4be8b0d63dd74bb4a97abe32df1517ac.png)

### 3.进入官网[演示 - uCharts跨平台图表库](https://www.ucharts.cn/v2/#/demo/index "演示 - uCharts跨平台图表库")，找一个示例代码测试一下，是否可以成功应用

![](https://i-blog.csdnimg.cn/direct/7b1b89fa70e64287886d4fce2f1d5bf0.png)

![](https://i-blog.csdnimg.cn/direct/a57725f628b14d81a281ad25d3a4b1c3.png)

> 因为这里使用的是vue2，如果你是vue3的项目，该段代码可能会有问题，建议可以先转换为vue3的写法
> 
> 小tip：复制网站里面的该段代码，让AI帮你改成vue3的写法

本案例使用的是vue3+ts的写法：

```html
<template>
  <view class="charts-box">
    <qiun-data-charts
      type="line"
      :opts="opts"
      :chartData="chartData"
    />
  </view>
</template>

<script lang="ts" setup>
import { ref, onMounted } from 'vue'

// 定义图表数据类型
interface ChartData {
  categories: string[]
  series: {
    name: string
    data: number[]
  }[]
}

// 响应式数据
const chartData = ref<ChartData>({
  categories: [],
  series: []
})

// opts 配置（可抽离到 config 文件）
const opts = {
  color: ["#1890FF", "#91CB74", "#FAC858", "#EE6666", "#73C0DE", "#3CA272", "#FC8452", "#9A60B4", "#ea7ccc"],
  padding: [15, 10, 0, 15],
  enableScroll: false,
  legend: {},
  xAxis: {
    disableGrid: true
  },
  yAxis: {
    gridType: "dash" as const,
    dashLength: 2
  },
  extra: {
    line: {
      type: "straight" as const,
      width: 2,
      activeType: "hollow" as const
    }
  }
}

// 模拟请求服务器数据
const getServerData = () => {
  setTimeout(() => {
    const res: ChartData = {
      categories: ["2018", "2019", "2020", "2021", "2022", "2023"],
      series: [
        { name: "成交量A", data: [35, 8, 25, 37, 4, 20] },
        { name: "成交量B", data: [70, 40, 65, 100, 44, 68] },
        { name: "成交量C", data: [100, 80, 95, 150, 112, 132] }
      ]
    }
    chartData.value = JSON.parse(JSON.stringify(res)) // 深拷贝模拟响应
  }, 500)
}

// 页面加载完成后获取数据
onMounted(() => {
  getServerData()
})
</script>

<style scoped>
.charts-box {
  width: 100%;
  height: 300px;
}
</style>
```

你可以将 `opts` 抽离成独立文件，比如 `config/chartConfig.ts`：

```javascript
// config/chartConfig.ts
export const lineChartOpts = {
  color: ["#1890FF", "#91CB74", "#FAC858", "#EE6666", "#73C0DE", "#3CA272", "#FC8452", "#9A60B4", "#ea7ccc"],
  padding: [15, 10, 0, 15],
  enableScroll: false,
  legend: {},
  xAxis: { disableGrid: true },
  yAxis: { gridType: "dash", dashLength: 2 },
  extra: { line: { type: "straight", width: 2, activeType: "hollow" } }
}
```

然后在组件中导入：

```javascript
import { lineChartOpts } from '@/config/chartConfig'
const opts = lineChartOpts
```

### 4.效果展示（微信小程序端）

![](https://i-blog.csdnimg.cn/direct/2a68356da6d64302abf6d29f4b1d2eb9.png)

这样一个简单的折线图就在uniapp项目里面实现了
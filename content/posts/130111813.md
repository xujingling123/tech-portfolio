---
title: "数据结构|单链表的逆置（详解）头插法"
date: "2023-04-12 17:24:50"
description: "后续循环也是以此类推。"
tags: ["数据结构", "链表", "算法", "c语言", "开发语言"]
viewCount: 991
articleId: "130111813"
sourceUrl: "https://blog.csdn.net/jingling555/article/details/130111813"
cover: "https://i-blog.csdnimg.cn/blog_migrate/cover/61d899415b46d70453b84c003bcd8665.png"
---头插法：

分析：

第一次循环：

![](https://i-blog.csdnimg.cn/blog_migrate/61d899415b46d70453b84c003bcd8665.png)

第二次循环：

![](https://i-blog.csdnimg.cn/blog_migrate/acde54651b9609aa86abdbd97786f1db.png)

后续循环也是以此类推 

代码：

```cpp
typedef struct LinkList {
    int data;
    struct LinkList* next;

}LinkList;
//单链表的逆置，头插法
LinkList reverseList1(LinkList* L) {
    //定义两个指针
    LinkList* p, * q;
    p = L->next;
    L->next = NULL;
    while(p != NULL) {
        q = p->next;//先存储p的下一个结点，方便进行下一次循环
        p->next = L->next;//把p结点的next域指向L的下一个结点
        L->next = p;//然后将头结点指向p，就让p成为了首元素
        p = q;//将下一个要处理的结点赋给p

    }
    return L;

}
```